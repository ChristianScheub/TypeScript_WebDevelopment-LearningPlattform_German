import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.scheub.webentwicklung',
  appName: 'Webentwicklung lernen',
  webDir: 'build'
};

export default config;
